[project]
name = "symmetries-workspace"
version = "0.0.0"
description = "uv workspace root — not a real package"
requires-python = ">=3.11"

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pre-commit>=4.0.0",
]
